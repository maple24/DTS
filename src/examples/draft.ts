import { SomeClass, someFunction } from 'library-name';

const instance = new SomeClass();
someFunction(instance);


// ......
console.log(Math.PI);

getArrayLength([1, 2, 3])


const person: Person = {
    name: 'maple', age: 25
}

const employee: Employee = {
    experience: 10,
}